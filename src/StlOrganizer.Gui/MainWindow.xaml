<ui:FluentWindow x:Class="StlOrganizer.Gui.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:gui="clr-namespace:StlOrganizer.Gui"
                 d:DataContext="{d:DesignInstance Type=gui:MainWindowViewModel}"
                 mc:Ignorable="d"
                 Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}"
                 Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"
                 Height="380" Width="856"
                 WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel>
            <ui:TitleBar Title="STL Organizer" />
            <ui:NavigationView x:Name="NavigationView"
                               PaneDisplayMode="LeftMinimal"
                               IsBackButtonVisible="Collapsed"
                               AlwaysShowHeader="True"
                               IsPaneOpen="False"
                               MenuItemsSource="{Binding MenuItems, Mode=OneWay}" />
        </StackPanel>

    </Grid>
</ui:FluentWindow>